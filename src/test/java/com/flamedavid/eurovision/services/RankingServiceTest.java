package com.flamedavid.eurovision.services;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class RankingServiceTest {

    @InjectMocks
    private RankingService rankingService;

    @Test
    void calculateEurovisionAccuracyScore() {
//        var voteEntry1 = new VoteEntryDTO(CountryEnum.ALBANIA, 12);
//        int resp = rankingService.calculateEurovisionAccuracyScore(List.of(voteEntry1), List.of(voteEntry1));
//        assertEquals(3, resp);
//
//        var voteEntry2 = new VoteEntryDTO(CountryEnum.ARMENIA, 12);
//        resp = rankingService.calculateEurovisionAccuracyScore(List.of(voteEntry1), List.of(voteEntry2));
//        assertEquals(0, resp);
//
//        var voteEntry3 = new VoteEntryDTO(CountryEnum.ARMENIA, 10);
//        var voteEntry4 = new VoteEntryDTO(CountryEnum.AUSTRALIA, 10);
//        resp = rankingService.calculateEurovisionAccuracyScore(List.of(voteEntry1, voteEntry3),
//            List.of(voteEntry2, voteEntry4));
//        assertEquals(2, resp);
    }
}
